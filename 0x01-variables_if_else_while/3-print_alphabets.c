#include <stdio.h>
/**
*main - entrt point
*Return: always 0(success)
*/
int main(void)
{
int ch;
fir (ch = 97; ch <= 122; ch++)
{
putchar(ch);
}
for (ch = 65; ch <= 90; ch++)
{
putchar(ch);
}
putchar(10);
return (0);
}
